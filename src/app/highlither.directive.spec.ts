import { HighlitherDirective } from './highlither.directive';

describe('HighlitherDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlitherDirective();
    expect(directive).toBeTruthy();
  });
});
